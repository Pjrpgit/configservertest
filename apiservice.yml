server:
 port: 8084
eureka:
 instance:
  hostname: localhost
management:
 endpoints:
  enabled-by-default: false
 endpoint: 
  info:
   enabled: true
  health:
   enabled: true
  shutdown:
   enabled: true
